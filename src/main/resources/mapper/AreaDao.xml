<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lgk.o2o.dao.AreaDao">
	<select id="queryArea" resultType="com.lgk.o2o.entity.Area">
		select area_id as 区域ID,area_name as 区域名,priority as 权重,create_time as
		创建时间,last_edit_time as 最后一次更改时间
		from tb_area
		order by priority desc
	</select>
</mapper>